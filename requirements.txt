# Core — required.
PySide6 >= 6.6
opencv-python >= 4.8
numpy >= 1.24

# Console output. Required so the default install gets readable tracebacks, but
# every use site falls back to plain text if the import fails.
rich >= 13.0
rich-argparse >= 1.5

# Model backends — optional. Without them the YOLO and SAM plugins simply report
# themselves unavailable in View > Plugins, with the install command to fix it.
# Install with:  pip install -r requirements.txt -r requirements-models.txt
